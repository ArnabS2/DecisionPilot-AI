# ============================================================
# DecisionPilot AI — Python dependencies
# ============================================================

# ── Core app framework ───────────────────────────────────────
streamlit>=1.35.0

# ── AI / LLM ────────────────────────────────────────────────
anthropic>=0.28.0          # Claude API (analyzer, scorer, recommendation, simulator)
openai>=1.30.0             # Optional: swap-in or multi-model support

# ── Data processing ─────────────────────────────────────────
pandas>=2.2.0
numpy>=1.26.0

# ── PDF report generation ────────────────────────────────────
reportlab>=4.2.0           # report_generator.py — PDF output

# ── Visualisation (Streamlit charts & custom plots) ─────────
plotly>=5.22.0
matplotlib>=3.9.0
altair>=5.3.0              # Optional: declarative charts in Streamlit

# ── Utilities ────────────────────────────────────────────────
python-dotenv>=1.0.0       # Load ANTHROPIC_API_KEY from .env
pydantic>=2.7.0            # Data validation for module I/O contracts
tenacity>=8.3.0            # Retry logic for API calls
loguru>=0.7.2              # Structured logging

# ── Development / testing (not required in production) ───────
pytest>=8.2.0
pytest-cov>=5.0.0
